=====================================

Originally asked ChatGPT to create a simple html with ingredient input(keyword), dietary restriction input(keyword), and a server for the Open AI api(keyword) to work off of. Purpose was to give as a foundation to work off of as detail server and api integration or foreign to us. There was a spot where the AI failed to match output border to the input border size so that had to be fixed. We learned a lot about how HTML interacts with Backend Javascript.

=====================================

Then we had ChatGPT show us how to create html tabs(keyword) and added the remaining tabs and html files manually. Not much needed to be done here as it was simply a quicker way to find out a basic feature while not using stack overflow.

=====================================

Finally once the files were generated the fat was trimmed and we once again asked ChatGPT to make the result aesthetically(keyword/purpose) pleasing.

This basically made a bunch of settings in the css style file.

Many small optimizations had to be made and a manual placing of specific graphics like the login had to be done manually.

=====================================

Logo was also generated Using ChatGPT